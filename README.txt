# progam-design
这个工程完成一些基本程序，主要包括C/C++，数据结构，shell脚本，IPC，网络编程等，方便工作中对程序的复用。
注册界面过程
1.在head.h中的enum menu_type枚举中添加新增加的界面ID；
2.在head.h中创建对应界面的各个元素的枚举类型，例如；
3.在menu.c中创建指针数组，每个数组元素指向一条界面显示信息；
4.在menu.c中创建页面处理函数，处理对应项；
5.将指针数组和页面处理函数分别注册；
