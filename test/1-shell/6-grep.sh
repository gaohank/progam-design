#!/bin/bash 
#grep -r 递归
#grep -n 行号
#grep -i 不区分大小写
#grep -v 反向选择，除了
#^[a-z]  以字符a-z开头
#[0-9]$  以字符0-9结尾
#[^d]    非d的符号
#a\{2\}  a出现2次
#a\{2,\} a至少出现2次
#a\{2,5\}a出现2-5次
#+ * " ' { } $ . [] 等符号需要加'\'
#正则中o*表示有零个或多个o，oo*表示至少有一个o
